<p>YO!</p>
<button {{action 'scrapeVotoAPI'}}>scrapeVotoAPI</button>

<ul>
	{{#each model.places as |place|}}
		<li>{{place.name}} - #{{place.hashtag}}</li>
		{{#each place.photos as |photo|}}
			<p>{{photo.url}}</p>
		{{/each}}
	{{/each}}
</ul>
